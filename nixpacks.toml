[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10.24.0", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @ux-testing/api build || true"]

[start]
cmd = "pnpm --filter @ux-testing/api start"
